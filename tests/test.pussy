
func factorial(n) {
    if (n <= 1) {
        return 1;
    }
    return n * factorial(n - 1);
}

print factorial(6);


var sc = b"\x90\x90\xCC";
print len(sc);
print hex(0xDEAD);

var buf = alloc(0x1000);
write(buf, sc, 3);
exec(buf);
free(buf);